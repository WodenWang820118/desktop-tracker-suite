{
  "name": "@org/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test review/**/*.test.ts review-gate/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --pretty false && tsc -p tsconfig.spec.json --pretty false"
  }
}
